
# NÃO utilize este arquivo em produção.
# Crie um .env.local baseado neste arquivo.

# copie para .env
DB_USER=seu_usuario
DB_PASSWORD=sua_senha
JWT_SECRET=seu_jwt_secret 
MINIO_USER=seu_usuario     #minimo 3 caracters
MINIO_PASSWORD=sua_senha   #minino 8 caracteres

# obs: client_id do Google pode ser público.
# copie para agenday-frontend/.env
VITE_API_URL=http://localhost:8080/api/v1/
VITE_GOOGLE_CLIENT_ID=seu_google_client_id

# NÃO utilize este arquivo em produção.
# Crie um .env.local baseado neste arquivo.

# copie para .env
DB_USER=seu_usuario
DB_PASSWORD=sua_senha
JWT_SECRET=seu_jwt_secret 

# obs: client_id do Google pode ser público.
# copie para agenday-frontend/.env
VITE_API_URL=http://localhost:8080/api/v1/
VITE_GOOGLE_CLIENT_ID=seu_google_client_id